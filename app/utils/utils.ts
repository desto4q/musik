import {style as tw} from 'twrnc';
import {tailwind} from 'easycolors';
let themeObj = {
  background: tailwind.neutral[800],
  text: tailwind.slate[200],
};
let colors = tailwind;
export {tw, themeObj, colors};
